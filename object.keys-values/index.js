const student = {
    name: "Pietro",
    age: 16,
    grade: "2A",
    school: "Liceo Classico"
}

const key = Object.keys(student);

console.log(key);

// Esercizio successivo Object.values() - stesso oggetto

const value = Object.values(student);

console.log(value);